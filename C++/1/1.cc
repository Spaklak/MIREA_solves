#include <iostream>

using namespace std;

int main(){
    long int a, b;
    char c;
    cin >> a;
    cin >> c;
    cin >> b;
    cout << a + b << c << endl; // cout на вывод, endl перевод на новую строку
    cout << a;
    return 0;
}